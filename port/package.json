{
  "name": "palmier-port",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-platform rewrite of Palmier Pro (Windows/Linux/macOS) on Tauri + React.",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @palmier/desktop tauri dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.33.0"
}
